---
layout: base.njk
---
<article class="location">
  <div class="location__hero" data-hero>
    <div class="location__hero-scrim">
      <p class="location__eyebrow">{{ type }} &middot; {{ region }}{% if demolished %} &middot; <span class="location__demolished">Demolished</span>{% endif %}</p>
      <h1 class="location__title">{{ title }}</h1>
      {% if dateExplored %}<p class="location__date">Explored {{ dateExplored | readableDate }}</p>{% endif %}
    </div>
  </div>

  {% if blurb %}
  <div class="location__blurb">
    {{ blurb | safe }}
  </div>
  {% endif %}

  <div class="location__gallery" data-gallery="{{ page.fileSlug }}">
    <p class="location__loading">Loading photos&hellip;</p>
  </div>

  <p class="location__back"><a href="{{ '/' | url }}">&larr; Back to all locations</a></p>
</article>
