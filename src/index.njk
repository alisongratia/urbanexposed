---
layout: base.njk
title: null
---
<section class="hero">
  <h1>{{ site.title }}</h1>
  <p>{{ site.description }}</p>
</section>

<section class="grid">
  {% for location in collections.locations %}
  <a class="card" href="{{ location.url | url }}">
    <div class="card__image" data-card-slug="{{ location.fileSlug }}"></div>
    <div class="card__body">
      <h2 class="card__title">{{ location.data.title }}</h2>
      <p class="card__meta">{{ location.data.type }} &middot; {{ location.data.region }}</p>
    </div>
  </a>
  {% endfor %}
</section>
